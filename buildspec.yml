version: 0.2

phases:
  install:
    commands:
      # Update libs
      - echo "Hiii"
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build:ci
      - npm run test:ci
  post_build:
    commands:
      - sudo zip -r s3-artifacts.zip scripts appspec.yml dist/angular-devops
    #  - aws s3 sync ./dist/angular-devops s3:// --acl 'public-read' --delete
artifacts:
  files:
    - 'appspec.yml'
    - 'scripts/after.sh'
    - 'dist/angular-devops/**/*' 
  #discard-paths: yes
  #base-directory: 'dist/angular-devops' 


