version: 0.2

phases:
  install:
    commands:
      # Update libs
      - echo "Hiii"
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build:ci
      - npm run test:ci
  #post_build:
   # commands:
    #  - aws s3 sync ./dist/angular-devops s3:// --acl 'public-read' --delete

artifacts:
  files:
    - '**/*.html'
    - '**/*.js'
    - '**/*.css'
    - '**/*.ico'
    - 'appspec.yml'
    - 'scripts/after.sh'
  discard-paths: yes
  base-directory: 'dist/angular-devops'
